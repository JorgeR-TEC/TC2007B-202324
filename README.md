Proyecto que incluye el front y el backend


Para instalar, ejecutar:

    cd backend
    npm install package.json
    cd ..
    cd frontend
    npm install package.json
    cd ..

Para ejecutar, correr
    Desde la carpeta backend
        node index.js
    Desde la carpeta frontent
        npm start
